[project]
name = "grok-sulfide"
version = "1.0.0"
description = "Standalone Grok registrar with bundled Hotmail and CPA/OIDC support"
requires-python = ">=3.12,<3.14"
dependencies = [
    "DrissionPage>=4.1",
    "curl_cffi>=0.7",
]

[tool.uv]
package = false
